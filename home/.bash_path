# Append each of the following paths to $PATH if they exist and aren't already
# in $PATH.
for p in "$HOME/bin" "$HOME/.cargo/bin" "$GOPATH/bin"; do
  if [[ -d "$p" && ":$PATH:" != *":$p"* ]]; then
    export PATH="$PATH:$p"
    unset p
  fi
done
